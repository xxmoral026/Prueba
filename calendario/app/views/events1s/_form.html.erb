<%= form_with(model: events1, local: true) do |form| %>
  <% if events1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(events1.errors.count, "error") %> prohibited this events1 from being saved:</h2>

      <ul>
      <% events1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :events1_title %>
  </div>

  <div class="field">
    <%= form.label :start %>
    <%= form.date_select :start, id: :events1_start %>
  </div>

  <div class="field">
    <%= form.label :end %>
    <%= form.date_select :end, id: :events1_end %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
